{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/independentid/IDQL-specs/schema/idql-condition.schema.json",
  "title": "IDQL Condition Type",
  "description": "Schema definition for an IDQL `condition` clause",
  "type": "object",
  "properties": {
    "$comment": "This definition is just a placeholder for a condition TO BE DEFINED",
    "rule": {
      "type": "string",
      "description": "A filter defining a matching condition based on RFC7644 Sec 3.4.2.2"
    },
    "action": {
      "enum": ["allow","deny","audit","disabled"],
      "description": "The outcome desired if condition is a match (default='allow')"
    }

  }
}